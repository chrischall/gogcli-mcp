{
  "name": "gogcli-mcp-monorepo",
  "private": true,
  "version": "2.29.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@chrischall/mcp-connector": "^1.3.0",
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-oauth-provider": "^0.10.3",
    "@cloudflare/workers-types": "^5.20260827.1",
    "@types/node": "^26.4.0",
    "agents": "^0.19.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.126.0"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "node scripts/check-runner-gog-version.mjs && node scripts/run-tests.js",
    "test:workspaces": "npm test --workspaces",
    "test:runner": "npm test --prefix fly-gog-runner",
    "check:runner-gog-version": "node scripts/check-runner-gog-version.mjs",
    "typecheck": "npm run typecheck --workspaces",
    "publish:all": "npm run build && npm publish --access public --workspaces",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "worker:test": "vitest run --config vitest.workers.config.mts"
  },
  "overrides": {
    "agents": {
      "@modelcontextprotocol/sdk": "^1.30.0"
    }
  }
}
