{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  // The Worker entry is compiled by wrangler/esbuild, not the stdio build.
  "exclude": ["node_modules", "dist", "src/worker.ts"]
}
